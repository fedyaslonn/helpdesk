apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10 # Grafana будет проверять папку каждые 10 сек
    options:
      path: /var/lib/grafana/dashboards
      